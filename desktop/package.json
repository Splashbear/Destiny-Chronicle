{
  "name": "destiny-chronicle-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop shell for Destiny Chronicle offline archives",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:web": "cd .. && npm run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
